{
  "name": "fullstack-app",
  "version": "1.0.0",
  "scripts": {
    "frontend": "cd fe && npm run dev",
    "backend": "cd be && nodemon server.js",
    "dev": "concurrently \"npm run frontend\" \"npm run backend\""
  },
  "devDependencies": {
    "concurrently": "^7.0.0"
  }
}
